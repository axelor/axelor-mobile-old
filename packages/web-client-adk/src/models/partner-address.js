
const model = 'com.axelor.apps.base.db.PartnerAddress';

const fields = ['address'];

const mapFields = {
  address: 'address',
};

const mapLabels = {
  address: 'Address',
};
export default {
  model,
  fields,
  mapFields,
  mapLabels,
};
